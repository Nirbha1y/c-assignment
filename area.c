#include<stdio.h>
#include<math.h>
#define pi 3.14
void sphere(){float a, r;
printf("Enter the radius");
scanf("%f",&r);
a =(are,r);
printf ("The area is %f",a);
}

float main(float are,float r){
	are= (4.0/3)*pi*(pow(r,2));
	return  0;

